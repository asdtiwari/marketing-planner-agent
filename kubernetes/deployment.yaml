apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-stack
spec:
  replicas: 1
  selector:
    matchLabels:
      role: app
  template:
    metadata:
      labels:
        role: app
    spec:
      containers:
      # --- Backend Container ---
      - name: backend
        image: asdtiwari/marketing-backend:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: marketing-config
        - secretRef:                  # <-- THIS IS THE MISSING PIECE!
            name: marketing-secrets
      # --- Frontend Container ---
      - name: frontend
        image: asdtiwari/marketing-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80